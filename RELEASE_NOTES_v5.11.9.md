# Inhouse Notes v5.11.9

- Replaces the failing legacy Pages publisher with an explicit, repository-owned GitHub Pages workflow.
- Keeps deployments in a single production queue so a newer push cannot cancel an upload GitHub has already accepted.
- Extends the Pages backend wait from the managed 10-minute cutoff to 30 minutes, preventing valid queued deployments from being canceled prematurely.
- Preserves the v5.11.8 scanner improvements: the lower square box remains highlighted in bright yellow throughout processing and colour correction uses the four printed colour circles.
- Retains clean white paper and vivid, brush-matched ink tones while the publishing repair is released as its own visible version.
